rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ================================
    // ðŸ”§ Helpers
    // ================================
    function isSignedIn() {
      return request.auth != null;
    }

    function isSelf(uid) {
      return request.auth != null && request.auth.uid == uid;
    }

    function isAdmin() {
      return request.auth != null && request.auth.token.admin == true;
    }

    // ================================
    // ðŸ”¥ POKÃ‰DEX ADMIN
    // ================================

    // EspÃ©cies base dos PokÃ©mon (Biblioteca)
    match /pokemonSpecies/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // ConfiguraÃ§Ãµes mutÃ¡veis de venda dos itens (loja)
    match /itemsConfig/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // ConfiguraÃ§Ãµes por versÃ£o do jogo
    match /pokedexConfig/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // Configuracao de encontros por bioma (definida no Pokedex/Admin)
    match /biomeEncounterConfig/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();

      // Subcolecoes por bioma: /groups e /individual
      match /{document=**} {
        allow read: if isSignedIn();
        allow write: if isAdmin();
      }
    }

    // Cadastro de biomas (admin/web)
    match /biomes/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Rotas entre biomas (acesso: KM, ticket, requisitos)
    match /biomeRoutes/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Regras dinâmicas de evolução configuradas pelo admin/web
    match /evolutionConfigRules/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Cadastro de NPCs (admin/web)
    match /npcs/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Cadastro de cenarios (admin/web)
    match /scenarios/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Cadastro de insignias (admin/web)
    match /badges/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Pool global de Pokemon liberados/abandonados para re-encontro em biomas
    match /releasedPokemonPool/{id} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && request.resource.data.sourceUid == request.auth.uid;
      allow update: if isAdmin() || (
        isSignedIn() &&
        resource.data.status == "active" &&
        request.resource.data.status == "captured"
      );
      allow delete: if isAdmin();
    }

    // Grupos de captura (configuracao coletiva)
    match /captureConfigGroups/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    // Movimentos (catÃ¡logo geral de golpes)
    match /moves/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // Learnset por espÃ©cie
    match /pokemonMoves/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // Habilidades
    match /abilities/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // Itens globais (CatÃ¡logo)
    match /items/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    match /vipPlans/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    match /monetizationProducts/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    match /ecoinPackages/{id} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    match /gyms/{gymId} {
      allow read: if isAdmin() || isSignedIn();
      allow create: if isSignedIn() && gymId == request.auth.uid && request.resource.data.ownerUid == request.auth.uid;
      allow update, delete: if isAdmin() || (isSignedIn() && gymId == request.auth.uid);

      match /storage/{entryId} {
        allow read: if isAdmin() || isSignedIn();
        allow write: if isAdmin() || (isSignedIn() && gymId == request.auth.uid);
      }

      match /mainTeam/{entryId} {
        allow read: if isAdmin() || isSignedIn();
        allow write: if isAdmin() || (isSignedIn() && gymId == request.auth.uid);
      }

      match /{document=**} {
        allow read: if isAdmin() || (isSignedIn() && gymId == request.auth.uid);
        allow write: if isAdmin() || (isSignedIn() && gymId == request.auth.uid);
      }
    }

    match /gymNames/{nameId} {
      allow read: if isAdmin() || isSignedIn();
      allow create: if isSignedIn() && request.resource.data.ownerUid == request.auth.uid;
      allow update, delete: if isAdmin() || (isSignedIn() && resource.data.ownerUid == request.auth.uid);
    }

    // ================================
    // ðŸ”¥ ELODEX â€” USUÃRIOS
    // ================================

    // Perfil e dados do app mobile
    match /players/{uid} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();

      // Todas as subcolecoes: characters, box, time, itens, pokeballs, etc.
      match /{document=**} {
        allow read:  if isSelf(uid) || isAdmin();
        allow write: if isSelf(uid) || isAdmin();
      }
    }

    match /players/{uid}/monetizationHistory/{entryId} {
      allow read: if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    match /players/{uid}/productEntitlements/{entryId} {
      allow read: if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    match /users/{uid} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    // HistÃ³rico financeiro principal (por usuÃ¡rio)
    match /users/{uid}/transactions/{txId} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    // âœ… collectionGroup(transactions) para admin
    match /{path=**}/transactions/{txId} {
      allow read: if isAdmin();
    }

    // Time principal
    match /users/{uid}/team/{slotId} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    // Box de armazenamento
    match /users/{uid}/box/{boxEntryId} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    // InventÃ¡rio de itens do jogador
    match /users/{uid}/inventory/{itemId} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    // HistÃ³rico econÃ´mico
    match /users/{uid}/economy/{eventId} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    // HistÃ³rico por PokÃ©mon
    match /users/{uid}/pokemonHistory/{histId} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    // ================================
    // ðŸ”¥ INSTÃ‚NCIAS DE POKÃ‰MON
    // ================================
    match /pokemonInstances/{pokeInstanceId} {
      allow read: if (isSignedIn() && isSelf(resource.data.ownerId)) || isAdmin();
      allow write: if (isSignedIn() && isSelf(request.resource.data.ownerId)) || isAdmin();
    }

    // ================================
    // ðŸ”¥ ESTATÃSTICAS
    // ================================
    match /users_stats/{uid} {
      allow read:  if isSelf(uid) || isAdmin();
      allow write: if isSelf(uid) || isAdmin();
    }

    match /pokemon_stats/{pokeId} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // ================================
    // ðŸ”¥ LOGS / MISSÃ•ES / EVENTOS
    // ================================

    match /encounters_log/{id} {
      allow read:  if false;
      allow write: if isSignedIn();
    }

    // âœ… CONFIGURAÃ‡ÃƒO ADMIN (NOVO) â€” MissÃµes & Eventos (Admin)
    // ColeÃ§Ã£o unificada apenas para CONFIGURAÃ‡ÃƒO
    // (nÃ£o Ã© execuÃ§Ã£o de jogo)
    match /missionsEvents/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // ColeÃ§Ãµes antigas (se vocÃª ainda usa em algum lugar)
    match /missions/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    match /events/{id} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    match /audit_logs/{id} {
      allow read:  if isAdmin();
      allow write: if isAdmin();
    }

    // ================================
    // ðŸ”¥ PRESENÃ‡A (futuro)
    // ================================
    match /presence/{uid} {
      allow read:  if isSignedIn();
      allow write: if false;
    }

    // BATTLE SYSTEM
    match /battlePresence/{id} {
      allow read: if isSignedIn();
      allow create, update: if isSignedIn() && request.resource.data.uid == request.auth.uid;
      allow delete: if isAdmin();
    }

    match /battleInvites/{id} {
      allow create: if isSignedIn() && request.resource.data.fromUid == request.auth.uid;

      allow read: if isAdmin() || (isSignedIn() && (
        resource.data.fromUid == request.auth.uid ||
        resource.data.toUid == request.auth.uid
      ));

      allow update: if isAdmin() || (isSignedIn() && (
        (resource.data.toUid == request.auth.uid && request.resource.data.status in ["accepted", "declined"]) ||
        (resource.data.fromUid == request.auth.uid && request.resource.data.status == "canceled")
      ));

      allow delete: if isAdmin();
    }

    match /battleRooms/{id} {
      allow read: if isSignedIn();
      allow create: if isSignedIn() && request.resource.data.ownerUid == request.auth.uid;
      allow update: if isAdmin() || (isSignedIn() && (
        resource.data.ownerUid == request.auth.uid ||
        resource.data.challengerUid == request.auth.uid ||
        (resource.data.status == "waiting_opponent" && request.resource.data.challengerUid == request.auth.uid)
      ));
      allow delete: if isAdmin() || (isSignedIn() && resource.data.ownerUid == request.auth.uid);
    }
  }
}
